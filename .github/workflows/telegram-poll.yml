name: Telegram Check Now

on:
  schedule:
    # Every 5 min at :03, :08, :13… (avoids GitHub queue rush at :00)
    - cron: "3-58/5 * * * *"
  workflow_dispatch:
  repository_dispatch:
    types: [check-now]

concurrency:
  group: telegram-poll
  cancel-in-progress: true

jobs:
  poll-telegram:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/cache@v4
        with:
          path: |
            last_state.json
            bot_offset.json
            ~/.cache/pip
          key: blr-fare-bot-state-v1

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: pip
          cache-dependency-path: requirements.txt

      - name: Install dependencies
        run: pip install -r requirements.txt

      - name: Process Telegram commands
        env:
          TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
          TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
        run: python telegram_poll.py
